import styled from 'styled-components'

export const Container = styled.span`
  font-size: 1.2rem;
  padding: .5rem 1.4rem;
  border-radius: 0.5rem;
  margin-right: .6rem;
  color: ${({theme}) => theme.COLORS.OPC_WHITE};
  background-color: #282124;
`